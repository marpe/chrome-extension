import { defineRunnerConfig } from "wxt";

export default defineRunnerConfig({
	binaries: {
		chrome: "C:/Program Files/Google/Chrome/Application/chrome.exe", // Use Chrome Beta instead of regular Chrome
	},
});
